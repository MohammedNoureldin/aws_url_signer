# aws-url-signer
This library signs AWS URLs with the given credentials in contrast to signing the header entries. This is particularly helpful to connect to WebSocket API Gateway with IAM authorizer.
